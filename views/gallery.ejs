<%- include("partials/header") %>

<div class="container">

    <div class="gallery">
        <% instafeed.forEach((data) => { %>
        <li><img src="<%= data.url %>" alt=""></li>
        <% }); %>
    </div>
</div>



<%- include("partials/footer") %>